$(document).ready(function () {
        // 로고 SVG -> LOGIN 화면에서 BLACK으로 색상변경
        $('.h_svg').attr('fill', 'black')
        // 검색 SVG -> LOGIN 화면에서 BLACK으로 색상변경
        $('.search_svg').attr('style', 'fill:#000')

       
})